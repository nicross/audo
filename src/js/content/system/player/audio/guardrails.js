content.system.player.audio.guardrails = (() => {

  // TODO: Define synth and trigger when player is against left/right boundaries

  return {
    reset: function () {
      return this
    },
    update: function () {
      return this
    },
  }
})()
