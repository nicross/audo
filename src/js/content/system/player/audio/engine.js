content.system.player.audio.engine = (() => {

  // TODO: Build ascending shepard tone generator and tie rate to relativeVelocity

  return {
    reset: function () {
      return this
    },
    update: function () {
      return this
    },
  }
})()
